.DS_Store
.claude/settings.local.json
__pycache__/
build/

# Secrets — keys that must never ship to the browser or the repo.
.env

# Derived terrain cache for build_shade.py — regenerable, not worth history.
data/.elevation-cache.json
